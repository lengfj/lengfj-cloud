server:
  port: 8888
  servlet:
    context-path: /system-api

spring:
  application:
    name: system-api
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
#  profiles:
#    active: @profiles.active@
